@model EmployeeManagement.Models.Designation

@{
    ViewData["Title"] = "Delete Designation";
}

<div class="container mt-5 d-flex justify-content-center">
    <div class="card shadow-sm w-50">
        <div class="card-body text-center">
            <h2 class="fw-bold text-danger mb-3">Delete Designation</h2>
            <p class="text-muted">Are you sure you want to delete this designation?</p>
            <hr />

            <dl class="row justify-content-center">
                <dt class="col-sm-4 fw-bold">
                    @Html.DisplayNameFor(model => model.DesignationName)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.DesignationName)
                </dd>

                <dt class="col-sm-4 fw-bold">
                    @Html.DisplayNameFor(model => model.Department)
                </dt>
                <dd class="col-sm-6">
                    @Html.DisplayFor(model => model.Department.DepartmentName)
                </dd>
            </dl>

            <form asp-action="Delete" class="mt-4">
                <input type="hidden" asp-for="DesignationId" />
                <button type="submit" class="btn btn-danger me-2">
                    <i class="bi bi-trash"></i> Delete
                </button>
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i> Back to List
                </a>
            </form>
        </div>
    </div>
</div>
